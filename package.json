{
  "name": "minimap",
  "author": "lumine-code",
  "description": "A preview of the full source code.",
  "repository": "https://github.com/lumine-code/minimap",
  "bugs": {
    "url": "https://github.com/lumine-code/minimap/issues"
  },
  "keywords": [
    "overview",
    "preview",
    "sidebar",
    "thumbnail",
    "canvas",
    "decoration"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main.js",
  "scripts": {
    "test": "cd ../../lumine && npm run test:only -- ../pkg_lumine/minimap/spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "configSchema": {
    "autoToggle": {
      "title": "Show Minimap On Startup",
      "description": "Automatically show the minimap when Lumine starts.",
      "type": "boolean",
      "default": true
    },
    "displayMinimapOnLeft": {
      "title": "Display Minimap On Left",
      "description": "When enabled, the minimap is displayed on the left side of the editor instead of the right.",
      "type": "boolean",
      "default": false
    },
    "displayCodeHighlights": {
      "title": "Display Code Highlights",
      "description": "Render syntax-colored tokens in the minimap. Disabling this draws plain text and may improve performance.",
      "type": "boolean",
      "default": true
    },
    "displayQuickSettings": {
      "title": "Display Quick Settings",
      "description": "Show the quick-settings button on the minimap. Requires a restart to take effect.",
      "type": "boolean",
      "default": true
    },
    "disabledLayers": {
      "title": "Disabled Layers",
      "description": "Names of the marker layers this map does not draw. The scrollbar strip keeps its own list.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [
        "cursors",
        "highlight",
        "references",
        "search-panel"
      ]
    },
    "thresholdScale": {
      "title": "Threshold Scale",
      "description": "Multiplies every layer's own marker limit before it decides to draw nothing. The minimap shows fewer rows at once than the scrollbar strip, so a limit tuned for the strip is generous here.",
      "type": "number",
      "minimum": 0,
      "default": 1
    },
    "adjustMinimapWidthToSoftWrap": {
      "title": "Adjust Minimap Width To Soft Wrap",
      "description": "When soft wrap and soft wrap at preferred line length are both enabled, cap the minimap width to match the preferred line length.",
      "type": "boolean",
      "default": true
    },
    "charWidth": {
      "title": "Character Width",
      "description": "Width of each character block in the minimap, in pixels.",
      "type": "number",
      "minimum": 0.5,
      "default": 1
    },
    "charHeight": {
      "title": "Character Height",
      "description": "Height of each character block in the minimap, in pixels.",
      "type": "number",
      "minimum": 0.5,
      "default": 2
    },
    "interline": {
      "title": "Interline Spacing",
      "description": "Extra vertical space between lines in the minimap, in pixels.",
      "type": "number",
      "minimum": 0,
      "default": 1
    },
    "textOpacity": {
      "title": "Text Opacity",
      "description": "Opacity of the rendered text in the minimap. Lower values make the code less prominent.",
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "default": 0.6
    },
    "independentMinimapScroll": {
      "title": "Independent Minimap Scroll On Mouse Wheel",
      "description": "When enabled, scrolling the mouse wheel over the minimap moves the minimap independently without scrolling the editor. The minimap still syncs back whenever the editor itself is scrolled.",
      "type": "boolean",
      "default": false
    },
    "scrollSensitivity": {
      "title": "Independent Scroll Sensitivity",
      "description": "Scroll speed multiplier used when independent minimap scroll is enabled.",
      "type": "number",
      "default": 0.5
    },
    "redrawDelay": {
      "title": "Redraw Delay (ms)",
      "description": "Minimum time in milliseconds between minimap redraws triggered by text changes. Higher values improve responsiveness on large files; 0 redraws immediately on every change.",
      "type": "number",
      "minimum": 0,
      "maximum": 2000,
      "default": 300
    },
    "devicePixelRatioRounding": {
      "title": "Device Pixel Ratio Rounding",
      "description": "Round the device pixel ratio used for canvas rendering. Disable if you see blurry or misaligned pixels on fractional-DPI displays.",
      "type": "boolean",
      "default": true
    },
    "useHardwareAcceleration": {
      "title": "Use Hardware Acceleration",
      "description": "Use CSS transforms for positioning, which enables GPU compositing and reduces repaints.",
      "type": "boolean",
      "default": true
    },
    "maxTokensInOneLine": {
      "title": "Max Tokens Per Line",
      "description": "Maximum number of syntax tokens rendered per line. Reducing this can improve performance on files with very long lines.",
      "type": "integer",
      "default": 160
    }
  },
  "activationHooks": [
    "core:loaded-shell-environment"
  ],
  "viewProviders": [
    "minimapViewProvider"
  ],
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.7.0",
    "prettier": "^3.9.6"
  },
  "consumedServices": {
    "marker.layer": {
      "versions": {
        "^1.0.0": "consumeMarkerLayer"
      }
    }
  },
  "dependencies": {
    "@lumine-code/marker-host": "file:../../pkg_nodejs/marker-host",
    "@lumine-code/select-list": "https://github.com/lumine-code/select-list.git#a201dae6a0ea6954cb9a905be4a718af697d9f78"
  }
}
